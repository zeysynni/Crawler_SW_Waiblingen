# Copy this file to `.env` and fill in your real values.
# `.env` is gitignored and must never be committed.

# No API key needed — the crawler is fully deterministic (crawl4ai, no LLM).

# Optional — phone alerts with the end-of-run report (no-op if absent)
PUSHOVER_TOKEN=
PUSHOVER_USER=
